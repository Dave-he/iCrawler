{
  "name": "@icrawler/core",
  "version": "1.0.0",
  "description": "Core workflow engine based on n8n",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && tsc-alias -p tsconfig.build.json",
    "dev": "tsc-watch -p tsconfig.build.json --onCompilationComplete \"tsc-alias -p tsconfig.build.json\"",
    "clean": "rimraf dist .turbo",
    "format": "biome format --write .",
    "lint": "biome check .",
    "test": "jest"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "n8n-workflow": "^1.100.0",
    "@n8n/core": "workspace:*",
    "@n8n/config": "workspace:*",
    "@n8n/constants": "workspace:*",
    "winston": "^3.14.2",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.0",
    "@types/jest": "^29.5.3",
    "@types/node": "*",
    "@types/uuid": "^10.0.0",
    "jest": "^29.6.2",
    "rimraf": "^5.0.1",
    "tsc-alias": "^1.8.10",
    "tsc-watch": "^6.2.0",
    "typescript": "*"
  }
}